import setaPreta from "./imgs/setaPreta.png";

export default function infoUser() {
  return (
    <main id="InfoMain">
      <section className="secaoInfo">
        <p className="textoInfoLabel"> Nome</p>
        <p className="textoInfo"> Arthur</p>
        <img src={setaPreta} className="setaPreta" />
      </section>

      <section className="secaoInfo">
        <p className="textoInfoLabel"> Nome</p>
        <p className="textoInfo"> Arthur</p>
        <img src={setaPreta} className="setaPreta" />
      </section>

      <section className="secaoInfo">
        <p className="textoInfoLabel"> Nome</p>
        <p className="textoInfo"> Arthur</p>
        <img src={setaPreta} className="setaPreta" />
      </section>

      <section className="secaoInfo">
        <p className="textoInfoLabel"> Nome</p>
        <p className="textoInfo"> Arthur</p>
        <img src={setaPreta} className="setaPreta" />
      </section>
    </main>
  );
}
